# 백엔드 아키텍처 문서

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프레임워크 | Spring Boot 4.0.2 |
| 언어 | Java 21 |
| 빌드 도구 | Maven |
| 데이터베이스 | Oracle Database XE (XEPDB1) |
| 인증 방식 | JWT (HMAC-SHA256) + Kakao OAuth2 |
| API 문서 | SpringDoc OpenAPI 3.0 (Swagger UI) |
| 서버 포트 | 8080 |
| 컨텍스트 경로 | `/api` |

---

## 2. 디렉토리 구조

```
backend/
├── pom.xml
├── uploads/                          # 업로드된 파일 로컬 저장 디렉토리
└── src/main/java/com/example/backend/
    ├── BackendApplication.java       # 진입점
    ├── album/                        # 포토 앨범 기능
    ├── albumphoto/                   # 앨범-사진 매핑 (M:N)
    ├── albumtag/                     # 앨범 태그
    ├── auth/                         # 인증/인가 (로그인, 회원가입, 이메일 인증)
    ├── badge/                        # 뱃지(달개) 및 반응 시스템
    ├── friend/                       # 글벗(친구) 관리
    ├── fss/                          # 금융감독원 API 프록시
    ├── global/                       # 전역 설정 (Security, JWT, CORS, Swagger)
    ├── notification/                 # 사용자 알림
    ├── photo/                        # 사진 업로드 및 저장
    ├── post/                         # 게시글/스냅
    ├── qna/                          # Q&A 게시판
    └── user/                         # 사용자 프로필 및 설정
```

---

## 3. 기술 스택 및 주요 의존성 (pom.xml)

| 의존성 | 버전 | 역할 |
|--------|------|------|
| spring-boot-starter-parent | 4.0.2 | Spring Boot 기반 프레임워크 |
| spring-boot-starter-webmvc | (inherited) | Web MVC |
| spring-boot-starter-data-jpa | (inherited) | JPA/Hibernate ORM |
| spring-boot-starter-security | (inherited) | Spring Security |
| spring-boot-starter-validation | (inherited) | Bean Validation |
| spring-boot-starter-mail | (inherited) | 이메일 전송 |
| spring-boot-starter-oauth2-client | (inherited) | Kakao OAuth2 |
| springdoc-openapi-starter-webmvc-ui | 2.3.0 | Swagger UI |
| jjwt-api / jjwt-impl / jjwt-jackson | 0.11.5 | JWT 생성/검증 |
| ojdbc11 | (inherited) | Oracle JDBC 드라이버 |
| lombok | (inherited) | 코드 자동 생성 |

---

## 4. 진입점 (BackendApplication.java)

```java
@SpringBootApplication(exclude = {
    SecurityAutoConfiguration.class,
    UserDetailsServiceAutoConfiguration.class
})
@EnableJpaAuditing
public class BackendApplication { ... }
```

- 기본 Spring Security 자동 설정 제외 → JWT 기반 커스텀 인증 사용
- `@EnableJpaAuditing` → `BaseEntity`의 `createdAt` / `updatedAt` 자동 관리

---

## 5. 데이터베이스 설정 (application.yml)

```yaml
datasource:
  url: jdbc:oracle:thin:@//100.91.129.24:1521/XEPDB1
  username: SCOTT
  driver-class-name: oracle.jdbc.OracleDriver

jpa:
  hibernate:
    ddl-auto: none           # 수동 DDL 관리
  show-sql: true
  properties:
    hibernate:
      default_batch_fetch_size: 100

app:
  upload:
    dir: uploads             # 로컬 파일 저장 경로

spring:
  mail:
    host: smtp.gmail.com
    port: 587                # STARTTLS

server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /api
```

---

## 6. 패키지별 구조 및 역할

### 6-1. global (전역 설정)

| 파일 | 역할 |
|------|------|
| `SecurityConfig.java` | Spring Security 설정, JWT 필터 등록, CORS 설정, OAuth2 Kakao 핸들러 |
| `JwtUtil.java` | JWT 토큰 생성(`createToken`), 파싱(`getUserIdFromToken`), 검증(`validationToken`) |
| `JWTAuthenticationFilter.java` | `OncePerRequestFilter` - `Authorization: Bearer {token}` 파싱 후 SecurityContext 설정 |
| `PasswordConfig.java` | BCryptPasswordEncoder 빈 등록 |
| `WebConfig.java` | `/uploads/**` 정적 리소스 핸들러, CORS |
| `SwaggerConfig.java` | OpenAPI Bearer Auth 스키마 설정 |
| `HttpCookieOAuth2AuthorizationRequestRepository.java` | Kakao OAuth2 인가 요청을 쿠키에 저장 |
| `BaseEntity.java` | `createdAt` / `updatedAt` 공통 필드 (모든 엔티티가 상속) |

**CORS 허용 출처:**
- `http://localhost:5173` (Vite 개발 서버)
- `http://192.168.0.44:5173` (로컬 네트워크)
- `http://10.0.2.2:8080` (Android 에뮬레이터)
- `capacitor://localhost` (모바일 앱)

**JWT 설정:**
- 알고리즘: HMAC-SHA256
- 만료: 24시간
- 응답 코드: `TOKEN_EXPIRED`, `INVALID_TOKEN`

### 6-2. auth (인증/인가)

**엔티티:**
- `EmailVerificationToken` - 이메일 인증 코드 (5분 만료)
- `PasswordResetToken` - 비밀번호 재설정 토큰 (5분 만료)

**주요 API:**
```
POST /api/auth/login                   → 이메일/패스워드 로그인
POST /api/auth/signup                  → 회원가입
POST /api/auth/logout                  → 로그아웃
POST /api/auth/kakao/login             → Kakao OAuth2 로그인
POST /api/auth/email/send-code         → 이메일 인증 코드 발송
POST /api/auth/email/verify-code       → 이메일 인증 코드 확인
POST /api/auth/email/send-reset-code   → 비밀번호 재설정 코드 발송
POST /api/auth/email/verify-reset-code → 재설정 코드 확인
POST /api/auth/reset-password          → 새 비밀번호 설정
```

### 6-3. user (사용자)

**엔티티:** `UserEntity`
- 주요 필드: `id`, `email`(unique), `password`(BCrypt), `username`, `profileImageUrl`, `provider`(LOCAL/KAKAO), `visibility`
- 관계: Post / Album / Badge / Friendship / Notification / Photo와 1:N

**주요 API:**
```
GET    /api/users/me                   → 내 프로필 조회
GET    /api/users/{userId}             → 특정 사용자 프로필
PUT    /api/users/me                   → 프로필 수정 (username, bio, visibility)
POST   /api/users/me/profile-image     → 프로필 이미지 업로드 (multipart)
PUT    /api/users/me/password          → 비밀번호 변경
DELETE /api/users/me                   → 계정 삭제
```

### 6-4. album (포토 앨범)

**엔티티:** `AlbumEntity`
- 주요 필드: `id`, `user`(FK), `title`, `bodyText`(CLOB), `recordDate`, `visibility`, `layoutType`, `createdAt`

**주요 API:**
```
POST /api/albums                       → 앨범 생성
GET  /api/albums/{albumId}             → 앨범 상세 조회
GET  /api/albums/layout-types          → 사용 가능한 레이아웃 타입 목록
GET  /api/albums/feed                  → 앨범 피드 (필터: type, friendsOnly, tag, page, size)
```

**2단계 생성 흐름:**
1. `photoService.uploadPhotos()` → `photoId` 배열 획득
2. `albumService.createAlbum(payload)` → photoIds, slotIndexes, tags 포함하여 앨범 생성

### 6-5. photo (사진 업로드)

**엔티티:** `PhotoEntity`
- 주요 필드: `id`, `user`(FK), `photoUrl`, `thumbUrl`, `createdAt`

**주요 API:**
```
POST /api/photos/upload                → 사진 업로드 (multipart/form-data, 최대 10MB)
GET  /api/photos/{photoId}             → 사진 메타데이터 조회
```

### 6-6. post (게시글/스냅) - 부분 구현

**엔티티:** `Post`
- 주요 필드: `id`, `user`(FK), `title`, `content`(CLOB), `type`(PHOTO/TEXT), `visibility`, `viewCount`
- `PostPhoto`: 게시글에 첨부된 사진
- `Tag`: 태그 시스템

**주요 API (부분 구현):**
```
GET    /api/posts                      → 게시글 목록 (필터: type, friendsOnly, tag)
GET    /api/posts/{id}                 → 게시글 상세
POST   /api/posts                      → 게시글 생성
PUT    /api/posts/{id}                 → 게시글 수정 (미구현)
DELETE /api/posts/{id}                 → 게시글 삭제 (미구현)
```

### 6-7. badge (뱃지/달개)

**엔티티:**
- `Badge` - 사용자 반응 기록 (user, album, badgeType 조합으로 unique)
- `BadgeType` - 반응 종류 마스터 (❤️좋아요, 😢슬퍼요, 😠화나요, 💪응원해요)

**주요 API:**
```
GET /api/badges/stats                  → 내 뱃지 통계
GET /api/badges/stats/{userId}         → 특정 사용자 뱃지 통계
GET /api/badges/ranking/global         → 전체 랭킹 (페이지네이션)
GET /api/badges/ranking/friends        → 글벗 랭킹
GET /api/badges/types                  → 모든 뱃지 종류 조회
```

**초기 데이터 (DB 수동 삽입 필요):**
```sql
INSERT INTO BADGE_TYPES VALUES (1, '좋아요', '❤️', 1);
INSERT INTO BADGE_TYPES VALUES (2, '슬퍼요', '😢', 2);
INSERT INTO BADGE_TYPES VALUES (3, '화나요', '😠', 3);
INSERT INTO BADGE_TYPES VALUES (4, '응원해요', '💪', 4);
```

### 6-8. friend (글벗/친구)

**엔티티:** `Friendship`
- 주요 필드: `id`, `fromUser`(FK), `toUser`(FK), `status`(PENDING/ACCEPTED/REJECTED/BLOCKED), `createdAt`, `updatedAt`

**주요 API:**
```
GET    /api/friends                    → 내 글벗 목록 (ACCEPTED)
GET    /api/friends/pending            → 받은 글벗 신청 목록
POST   /api/friends/request            → 글벗 신청 발송
POST   /api/friends/{friendshipId}/accept → 신청 수락
POST   /api/friends/{friendshipId}/reject → 신청 거절
DELETE /api/friends/{friendId}         → 글벗 삭제
GET    /api/friends/search?q={query}   → 사용자 이름으로 검색
```

### 6-9. notification (알림)

**엔티티:** `NotificationEntity`
- 타입: `FRIEND_REQUEST`, `FRIEND_ACCEPT`, `BADGE`, `SYSTEM`
- 주요 필드: `id`, `user`(FK), `type`, `title`, `message`, `isRead`(0=미읽음), `createdAt`

**주요 API:**
```
GET    /api/notifications              → 전체 알림 목록
GET    /api/notifications/unread       → 미읽음 알림 수
PUT    /api/notifications/{id}/read    → 단건 읽음 처리
PUT    /api/notifications/read-all     → 전체 읽음 처리
DELETE /api/notifications/{id}         → 단건 삭제
DELETE /api/notifications              → 전체 삭제
```

### 6-10. qna (Q&A 게시판)

**엔티티:** `QnaPost` (BaseEntity 상속)
- 주요 필드: `id`, `user`(FK), `title`, `content`

**주요 API:**
```
GET    /api/qna?page=0&size=5&sort=createdAt,desc → 목록 (페이지네이션)
GET    /api/qna/{id}                               → 상세 조회
POST   /api/qna                                    → 작성
PUT    /api/qna/{id}                               → 수정
DELETE /api/qna/{id}                               → 삭제
```

### 6-11. fss (금융감독원 API 프록시)

**역할:** 프론트엔드가 직접 FSS OpenAPI를 호출하면 CORS 오류 + API 키 노출 → 백엔드가 프록시 역할 수행

**주요 API:**
```
GET /api/fss/list?apiType={type}&startDate={YYYYMMDD}&endDate={YYYYMMDD}
```
- 외부 API: `http://fss.or.kr/fss/kr/openApi/api/v1/disclosure`

---

## 7. 보안 구성

### 인증 흐름 (이메일/패스워드)

```
① POST /api/auth/login (email, password)
② AuthService: BCrypt.matches() 검증
③ JwtUtil.createToken(userId, email) → JWT 발급
④ 응답: { token, user }
⑤ 이후 요청: Authorization: Bearer {token} 헤더에 포함
⑥ JWTAuthenticationFilter: 토큰 파싱 → SecurityContext 설정
```

### 인증 흐름 (Kakao OAuth2)

```
① /oauth2/authorization/kakao (Spring Security 자동 처리)
② 사용자 → Kakao 로그인 → 인가 코드 발급
③ /login/oauth2/code/kakao 콜백 → SecurityConfig 핸들러
④ KakaoService: Kakao API로 사용자 정보 조회
⑤ DB 사용자 조회 또는 신규 생성
⑥ JWT 발급 → 프론트엔드로 리다이렉트 (token, isNewUser 쿼리파라미터)
```

### Public 엔드포인트 (인증 불필요)

```
/auth/**          로그인/회원가입/이메일 인증
/login/**         OAuth2 로그인 콜백
/oauth2/**        OAuth2 인가 요청
/v3/api-docs/**   Swagger JSON
/swagger-ui/**    Swagger UI
/fss/**           금융 API 프록시
/posts/**         게시글 조회 (공개)
/users/**         사용자 프로필 조회 (공개)
/badges/ranking/** 랭킹 조회 (공개)
/albums/**        앨범 조회 (공개)
/uploads/**       업로드 파일 접근 (정적 리소스)
/qna/**           Q&A 조회 (공개)
```

---

## 8. 엔티티 관계 (ERD 개요)

```
USERS (PK: USER_ID)
├── 1:N → POSTS (fk: USER_ID)
├── 1:N → ALBUMS (fk: USER_ID)
├── 1:N → BADGES (fk: USER_ID)
├── 1:N → FRIENDSHIPS (fk: FROM_USER_ID, TO_USER_ID)
├── 1:N → NOTIFICATIONS (fk: USER_ID)
├── 1:N → QNA_POSTS (fk: USER_ID)
└── 1:N → PHOTOS (fk: USER_ID)

ALBUMS (PK: ALBUM_ID, fk: USER_ID)
├── 1:N → ALBUM_PHOTOS (fk: ALBUM_ID) → PHOTOS (fk: PHOTO_ID)
└── 1:N → ALBUM_TAGS (fk: ALBUM_ID)

BADGES (PK: ID, fk: USER_ID, ALBUM_ID, BADGE_TYPE_ID)
└── N:1 → BADGE_TYPES (마스터 데이터: 좋아요/슬퍼요/화나요/응원해요)

POSTS (PK: POST_ID, fk: USER_ID)
├── 1:N → POST_PHOTOS (fk: POST_ID)
└── M:N → TAGS (via POST_TAG)

EMAIL_VERIFICATION_TOKEN (이메일 인증 코드, 5분 만료)
PASSWORD_RESET_TOKEN (비밀번호 재설정 토큰, 5분 만료)
```

| 엔티티 | PK 전략 | 주요 필드 |
|--------|---------|-----------|
| UserEntity | IDENTITY | email(unique), password, username, provider, visibility |
| AlbumEntity | IDENTITY | title, bodyText(CLOB), recordDate, visibility, layoutType |
| PhotoEntity | IDENTITY | photoUrl, thumbUrl, user |
| Post | SEQUENCE | title, content(CLOB), type, visibility, viewCount |
| Badge | SEQUENCE | user, album, badgeType (unique 조합) |
| BadgeType | SEQUENCE | name(unique), emoji, sortOrder |
| Friendship | IDENTITY | fromUser, toUser, status |
| NotificationEntity | IDENTITY | type, title, message, isRead |
| QnaPost | SEQUENCE | title, content, user |

---

## 9. 파일 업로드 설정

| 항목 | 값 |
|------|----|
| 저장 위치 | `./uploads/` (서버 로컬) |
| 정적 접근 | `GET /uploads/**` → 로컬 파일시스템 매핑 |
| 최대 파일 크기 | 10MB |
| 최대 요청 크기 | 20MB |
| 파일명 | UUID 기반 (충돌 방지) |

---

## 10. Swagger (OpenAPI)

| 항목 | 값 |
|------|----|
| UI 경로 | `http://localhost:8080/api/swagger-ui.html` |
| JSON 경로 | `http://localhost:8080/api/v3/api-docs` |
| 인증 방식 | Bearer Token (JWT) |
| 타이틀 | Backend API 문서 v1.0.0 |

---

## 11. 주요 설계 패턴

| 패턴 | 적용 위치 |
|------|-----------|
| 계층형 아키텍처 | Controller → Service → Repository |
| DTO 패턴 | 요청/응답 분리 (Request/Response DTO) |
| BaseEntity 상속 | createdAt/updatedAt 공통 필드 자동 관리 |
| JWT Stateless 인증 | 세션 없이 토큰으로만 인증 |
| BCrypt 해싱 | 비밀번호 단방향 암호화 |
| Lazy Loading | 연관관계 FetchType.LAZY (N+1 방지용 batch size 100) |
| 소프트 삭제 패턴 | 이메일 인증 토큰: `usedAt` 타임스탬프 기록 |
| 낙관적 업데이트 | 배지 토글: exist → delete, not exist → create |
| API 프록시 | FSS: CORS 우회 + API 키 보호 |
